<Project Sdk="Microsoft.Build.NoTargets">

  <PropertyGroup>
    <BuildArgs Condition="'$(BuildOS)' == 'windows'">$(BuildArgs) $(FlagParameterPrefix)NativeToolsOnMachine</BuildArgs>
  </PropertyGroup>

  <ItemGroup>
    <RepositoryReference Include="arcade" />
    <RepositoryReference Include="runtime" Condition="'$(BuildOS)' == 'windows'" />
  </ItemGroup>

</Project>
