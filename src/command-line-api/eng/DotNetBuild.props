<!-- When altering this file, include @dotnet/product-construction as a reviewer. -->
<Project>

  <PropertyGroup>
    <BuildArgs Condition="'$(DotNetBuildSourceOnly)' == 'true'">$(BuildArgs) /p:Projects="$(ProjectDirectory)sourcebuild.slnf"</BuildArgs>
  </PropertyGroup>

</Project>
