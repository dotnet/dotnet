<!-- When altering this file, include @dotnet/product-construction as a reviewer. -->
<Project>

  <PropertyGroup>
    <BuildArgs>$(BuildArgs) /p:Projects="$(ProjectDirectory)Microsoft.FSharp.Compiler.sln"</BuildArgs>
  </PropertyGroup>

</Project>
